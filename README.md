# SESI_LP
DISCIPLINA DE LOGICA E PROGRAMAÇÃO DA TURMA DO SESI 2026
git config --list
git config --global user.name "seu nome"
git config --global user.email "seu email"


git add "nome do arquivo"
git status
git push
git commit -m "descrição do commit"
git pull
